{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start:dev": "bun run prisma:generate && bun run --watch src/index.ts",
    "prisma:generate": "bun prisma generate"
  },
  "dependencies": {
    "@elysiajs/cookie": "^0.8.0",
    "@elysiajs/jwt": "^1.0.2",
    "@prisma/client": "5.15.0",
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "elysia": "latest",
    "jsonwebtoken": "^9.0.2",
    "prisma": "^5.15.0",
    "prisma-import": "^1.0.5"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/bun": "^1.2.3",
    "@types/jsonwebtoken": "^9.0.6",
    "bun-types": "latest"
  },
  "compilerOptions": {
    "typeRoots": [
      "./node_modules/@types",
      "./types"
    ]
  },
  "module": "src/index.js"
}
